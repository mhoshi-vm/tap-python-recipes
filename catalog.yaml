apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: python-simple
  description: python-catalog
  tags:
    - app-accelerator
    - java
    - spring
    - web
    - tanzu
  annotations:
    'backstage.io/kubernetes-label-selector': 'app.kubernetes.io/part-of=py-simple'
    'backstage.io/kubernetes-namespace': tap-dev01
spec:
  type: service
  lifecycle: experimental
  owner: default-team
